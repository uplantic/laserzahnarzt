---
const { variant = "primary", showUrgency = false, className = "" } = Astro.props;

const baseClasses = "inline-flex items-center justify-center font-semibold transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2";

const variantClasses = {
  primary: "px-8 py-4 text-lg bg-blue-600 text-white rounded-lg hover:bg-blue-700 focus:ring-blue-500 shadow-lg hover:shadow-xl transform hover:-translate-y-1",
  secondary: "px-6 py-3 text-base bg-white text-blue-600 border-2 border-blue-600 rounded-lg hover:bg-blue-50 focus:ring-blue-500",
  compact: "px-4 py-2 text-sm bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:ring-blue-500",
};

const currentVariantClass = variantClasses[variant] || variantClasses.primary;
---

<section class="bg-gray-50 py-16 w-full">
  <div class="px-6 text-center">
    <div class={className}>
      <p class="mb-6 text-base text-gray-700">Sie haben Fragen oder möchten einen Termin? Rufen Sie uns an, wir beraten Sie gern.</p>

      <a href="tel:+4951029013144" class={`${baseClasses} ${currentVariantClass}`} aria-label="Zahnarztpraxis anrufen für Terminvereinbarung">
        <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
          ></path>
        </svg>
        <div class="text-left">
          <div class={`${variant === "compact" ? "text-xs" : "text-sm"} opacity-90`}>Jetzt anrufen</div>
          <div class={`${variant === "compact" ? "text-sm" : "text-lg"} font-bold`}>05102 - 91 31 44</div>
        </div>
      </a>

      {
        variant === "primary" && (
          <p class="mt-3 text-sm text-gray-600">
            <>
              <strong>Schnelle Terminvergabe</strong>
              <br />
            </>
            Montags bis Freitags
          </p>
        )
      }
    </div>
  </div>
</section>
